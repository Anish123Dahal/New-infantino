import {db} from '@/lib/db';import {redis,safeRedis} from '@/lib/redis';import {ok,fail} from '@/lib/http';export const dynamic='force-dynamic';export async function GET(){try{await db.$queryRaw`SELECT 1`;const cache=await safeRedis(()=>redis.ping(),'degraded');return ok({status:'ready',database:'ok',redis:cache==='PONG'?'ok':'degraded'});}catch{return fail('NOT_READY','Database unavailable',503)}}
